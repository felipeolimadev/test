# 그 외 다른 사람이 나온 사진 없음
FILTER NOT EXISTS {
    ?target user:withPerson ?person .
    FILTER NOT EXISTS {
        VALUES ?origin { <TAKING_PICTURES_IRI> }
        ?origin user:withPerson ?person .
    }
}